<UserControl x:Class="Familiada.View.QuestionSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Familiada.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Question, Mode=OneWay}" Grid.Row="0" Grid.Column="0"/>
        <TextBlock Text="{Binding Timer, Mode=OneWay}" Grid.Row="0" Grid.Column="1"/>
        
        <TextBox Text="{Binding Answer, Mode=TwoWay}" Grid.Row="1" Grid.Column="0"/>
        <Button Name="checkButton" Grid.Row="1" Grid.Column="1"/>

    </Grid>
</UserControl>
